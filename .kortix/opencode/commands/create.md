---
description: Create media — images, video, audio, logos, Remotion, Replicate, TTS
agent: epic
---
Create mode for Epic Tech AI.

Brief: $ARGUMENTS

1. Load `epic-powers`, `media`, and generative skills (`replicate`, `elevenlabs`, `remotion`, `logo-creator`, …).
2. Spawn `@creator` (+ `@designer` if brand system needed).
3. Generate real files; iterate on quality; `show` previews.
4. If API secrets missing, name the exact dashboard secret required.
