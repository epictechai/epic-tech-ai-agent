---
description: Build a production website or webapp with strong design and QA
agent: epic
---
Site mode for Epic Tech AI.

Brief: $ARGUMENTS

1. Load `epic-powers`, `website-building` and/or `webapp` / `website-building-webapp`.
2. Spawn `@architect` if multi-page/app, then `@builder` + `@designer`.
3. Implement, run dev server, visual QA, fix issues.
4. `show` the live localhost URL.
5. Summarize stack, how to run, and residual risks.
