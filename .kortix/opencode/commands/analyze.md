---
description: Data/finance analysis — SQL, stats, variance, charts, xlsx models
agent: epic
---
Analyze mode for Epic Tech AI.

Question / dataset: $ARGUMENTS

1. Load `epic-powers` + data skills (`sql-queries`, `exploration`, `visualization`, `xlsx`, …).
2. Spawn `@analyst`.
3. Profile data → compute → chart → insights with methodology.
4. Never invent numbers. State assumptions.
