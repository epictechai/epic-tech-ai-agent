---
description: Visual/UI/brand design — logos, themes, slides aesthetics, design systems
agent: epic
---
Design mode for Epic Tech AI.

Brief: $ARGUMENTS

1. Load `epic-powers`, `design-foundations`, and relevant design skills.
2. Spawn `@designer` (and `@creator` if generative media is needed).
3. Deliver real files (CSS/HTML/SVG/PNG/slide theme), not only prose.
4. Visual QA: contrast, hierarchy, brand fit (Epic Tech AI).
5. Show previews with the `show` tool.
