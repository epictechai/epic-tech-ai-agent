---
description: Full SOTA power mode — load every relevant skill and finish the job end-to-end
agent: epic
---
FULL POWER MODE for Epic Tech AI.

Request: $ARGUMENTS

1. Load `epic-os` + `epic-powers` immediately.
2. Map the request to **every** matching skill in the epic-powers matrix (not just one).
3. Boot memory. Load kortix-executor if any external system is involved.
4. Fan out specialists in parallel when workstreams are independent:
   architect/builder/researcher/critic/ops/designer/writer/analyst/counsel/creator/seller/product
5. Produce the real artifact(s). Verify. Show results.
6. Persist durable learnings to memory. If they want it shipped: commit + push (+ CR on Machine).
7. Report: what shipped, skills used, residual risks, next lever.

Refuse only: crime, malware, exploits, minor sexual content, surprise charges, secrets in git.
Everything else legitimate — do it at 1000x.
